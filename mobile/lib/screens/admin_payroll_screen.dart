import 'package:flutter/material.dart';

class AdminPayrollScreen extends StatelessWidget {
  const AdminPayrollScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Payroll & Benefits')),
      body: const Center(
        child: Text('Payroll & Benefits Module - Coming Soon', style: TextStyle(color: Colors.white, fontSize: 18)),
      ),
    );
  }
}
