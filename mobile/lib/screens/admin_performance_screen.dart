import 'package:flutter/material.dart';

class AdminPerformanceScreen extends StatelessWidget {
  const AdminPerformanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Performance & Appraisals')),
      body: const Center(
        child: Text('Performance Module - Coming Soon', style: TextStyle(color: Colors.white, fontSize: 18)),
      ),
    );
  }
}
